
import "mocha";
import * as assert from "power-assert";

describe("updating @atomist/automation-client dependencies", () => {

    it.skip("updates package.json to the new version", done => {


    });

    it.skip("runs npm install to update package-lock.json", done => {

    })

});
